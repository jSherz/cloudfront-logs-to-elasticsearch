image: jsherz/base-java-container:openjdk-11-buster

stages:
  - lint
  - test

lint:
  tags:
    - docker
  stage: lint
  script:
    - sbt scalastyle
    - sbt test:scalastyle

test:
  tags:
    - docker
  stage: test
  script:
    - sbt coverage test coverageReport
